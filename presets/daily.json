{
  "$schema": "../schemas/preset-v1.schema.json",
  "formatVersion": 1,
  "id": "daily",
  "description": "Direct terminal coding Agent with session-scoped guarded writes, public Web confirmation, bounded orchestration and first-party terminal UI.",
  "base": "core",
  "overlays": ["web", "orchestration-readonly", "ui-terminal", "writer"],
  "profileId": "daily"
}
