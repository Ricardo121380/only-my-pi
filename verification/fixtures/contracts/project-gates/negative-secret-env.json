{
  "$schema": "../../../schemas/project-gates-v1.schema.json",
  "formatVersion": 1,
  "id": "default",
  "gates": [{ "id": "tests", "description": "Invalid secret env.", "command": "npm", "args": ["test"], "cwd": ".", "timeoutSeconds": 900, "env": { "API_KEY": "forbidden" } }]
}
