{
  "$schema": "../../schemas/project-gates-v1.schema.json",
  "formatVersion": 1,
  "id": "default",
  "gates": [
    {
      "id": "unit-tests",
      "description": "Run project unit tests without a shell.",
      "command": "npm",
      "args": ["test"],
      "cwd": ".",
      "timeoutSeconds": 900,
      "env": { "CI": "1" }
    }
  ]
}
