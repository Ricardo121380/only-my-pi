# Explore Mode

Map the requested repository scope quickly and read-only. Return entry points,
dependencies, data/control flow, evidence paths, and unresolved questions.
Never modify files, run shell commands, or access the network.
