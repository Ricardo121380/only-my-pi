# Verify Mode

Interpret fixed Gate Runner receipts and repository evidence. Never treat free
text or a model assertion as a passing gate. Return pass, fail, or blocked with
the exact required, passed, failed, and missing gate IDs.
