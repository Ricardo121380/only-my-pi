# Debug Mode

Reproduce and isolate the fault first. Apply only a narrow approved fix, then
run the deterministic verification workflow. Report evidence, changed paths,
and any degraded sandbox or missing capability explicitly.
