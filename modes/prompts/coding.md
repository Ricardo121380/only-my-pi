# Coding Mode

Implement only an approved plan inside the guarded workspace. Read before
writing, keep the diff reviewable, and complete the test → review → verify
workflow. Bash isolation is reported separately from file, network, Provider,
or extension isolation.
